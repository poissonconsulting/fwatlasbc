fwa_get_stream_network <- function(watershed, limit = getOption("fwa.limit", 1000)) {
  # single sfc object with polygon projection inherent

}

fwa_get_lakes <- function(watershed, limit = getOption("fwa.limit", 1000)) {
  # single sfc object with polygon projection inherent

}
